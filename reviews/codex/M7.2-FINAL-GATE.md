# M7.2 Final Gate

Date: 2026-08-02  
Authority: Codex  
Outcome: PASS — Prototype 0 complete

## Environment and clean checkout

- Windows 10.0.26200, PowerShell 5.1
- Git 2.54.0, Node 24.16.0, pnpm 10.14.0, Codex 0.146.0
- Final verification checkout: `C:\Users\BlueWhaleX\Downloads\aicl-m7-final-verified`
- The checkout uses a canonical long path. A separate probe reproduced blank
  Vite output when pnpm junctions were installed through the `BLUEWH~1` alias;
  the same commit and lockfile returned `/@vite/client` 200 from the long path.

## Automated gates

| Gate | Evidence | Result |
|---|---|---|
| Dependency install | `pnpm install --frozen-lockfile` | Pass |
| Installed provider | `codex:compatibility`; schema `b767c…df03` | Pass |
| Empty/idempotent DB | `pnpm migrate` twice; Core v4, Connector v2 | Pass |
| Repository | `pnpm check`; 66 tests plus typecheck, lint, Web build | Pass |
| Real provider | opt-in `real-codex.e2e.test.ts` | Pass |
| Whitespace | `git diff --check` | Pass |

The opt-in real test proves first delta/final response, `TURN_ALREADY_ACTIVE`,
interrupt, provider death, `outcome_unknown`, rejection on the lost Runtime,
new-Connector/new-provider resume using durable identity, exactly-once prior
command observation, and no automatic prompt replay.

## Browser acceptance

Playwright drove the actual browser at 1440×1000 against separate Web, Core, and
Connector processes using Codex App Server—not the mock provider.

- First Turn returned exactly `AICL_BROWSER_FINAL_OK`; reload reconstructed the
  durable operator/final response without resubmission.
- Decline changed the command activity to `DECLINED` and did not create
  `declined-proof.txt`.
- Approve-once created only the requested proof and displayed exit code 0,
  duration, and `AICL_APPROVED_OUTPUT`.
- File-change approval created one proof file. The inspector showed `+1/−0`,
  `/dev/null`, the added line, and working unified/side-by-side controls.
- A Connector restart advanced generation 1→2; refresh replayed completed state
  and a new provider process resumed without replaying an old prompt.
- Stop turn interrupted a 30-second command before `MUST_NOT_COMPLETE`; both
  Turn and command activity displayed `INTERRUPTED`.
- Final console inspection: 0 errors, 0 warnings.

## Deterministic recovery and boundary coverage

- Core restart, Connector loss/reconnect, commit-before-broadcast, sequence
  replay/deduplication, runtime fencing, passive expiry, approval races, and
  unknown-outcome convergence are covered by Core integration tests.
- Command batching, terminal-item reconciliation, FIFO journal receipts,
  compatibility, provider teardown, and Windows child-tree termination are
  covered by Connector tests.
- Large diffs cross the inline threshold into an authenticated, hash-verified,
  bounded-range artifact; unsafe media, traversal, unauthorized access,
  allocation excess, hostile Origin, forged Connector, bursts, and secret
  leakage are rejected by Core/protocol security tests.

## Final-gate repairs

1. Updated the real E2E lifecycle to replace a lost Connector instead of asking
   a terminal generation to respawn its provider.
2. Reconciled terminal items carried only by `turn/completed`, with deduplication
   when a normal `item/completed` also exists.
3. Added explicit `outcome_unknown` activity/file-change state and Core schema
   v4 fallback settlement at every terminal boundary.
4. Converted plain added/deleted file bodies into unified patches so counts and
   split review remain truthful.

No unresolved Prototype 0 blocker remains. Optional P1–P3 reviews do not block
this baseline.
