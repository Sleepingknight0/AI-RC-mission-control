# M8.5 Deployment Readiness

Status: **DEFERRED BY OPERATOR — not completed**

## Implemented

- `pnpm remote:configure` requires AICL stopped, requires Core on
  `127.0.0.1`, derives one exact `https://*.ts.net` Origin from online Tailscale
  status, configures only private Serve, verifies the loopback target, then
  persists the Origin atomically.
- `pnpm remote:status` distinguishes app, Connector, Tailscale, Serve, and
  Origin state. Doctor additionally distinguishes both SQLite databases and
  Codex compatibility.
- `Test-TailscaleRemote.ps1` refuses execution on the server device, tests
  production HTML, `/health`, the bodyless runtime bootstrap, and authenticated
  WSS, and never writes the one-time ticket to evidence.
- No implementation invokes `tailscale funnel`, opens a LAN listener, or changes
  Core/Connector away from loopback.

## Verification

```text
apps/host typecheck/test/lint             PASS (10 tests)
Test-TailscaleAutomation.ps1              PASS (fake online CLI/Serve)
pnpm build                                PASS
pnpm run doctor                           READY; every local and Tailscale check passes
pnpm remote:status                        app/Connector/Tailscale/Serve/Origin configured
occupied-port production start            FAILS CLOSED before child spawn
compiled production after dev shutdown    READY (supervisor/Core/Connector)
production stability checkpoints          PASS (3 s / 15 s / 30 s; stable PIDs)
remote HTTPS /health                      PASS through ts.net Serve
host-side Playwright remote preflight     PASS (HTML/runtime bootstrap/WSS)
pnpm check                                PASS (102 tests + lifecycle/Serve smokes)
git diff --check                          PASS
```

The first full check encountered `EPERM` while deleting an M8.3 temporary
process-test directory on Windows. The same unchanged targeted test passed
immediately, and the complete gate then passed. No test or timeout was weakened.

## Real deployment evidence

- Tailscale 1.98.10 is installed at its standard Windows Program Files path,
  signed in, online, and reports no local health warning.
- Private Serve maps `https://bluewhalex.tailc79f02.ts.net/` to
  `http://127.0.0.1:8787`; Funnel is disabled by construction.
- Core persists the exact HTTPS Origin. Doctor discovers the standard install
  even though it is not on PATH.
- A real dev/production port collision reproduced false readiness: the Host
  could observe the existing dev health endpoint before its compiled child exit
  event. Production now probes exclusive ownership of both ports and fails
  before spawning. After proving there were no active Turns, seven stale AICL
  dev launcher roots and their descendants were stopped. Compiled production
  then retained the same supervisor/Core/Connector PIDs through 3/15/30-second
  checkpoints.
- Certificate issuance recovered without another certificate command. Remote
  `/health` now succeeds over HTTPS. A host-side Playwright preflight loaded the
  production shell, completed runtime bootstrap and authenticated WSS, reported
  Core/Connector ready, emitted zero console warnings/errors, and stored no
  localStorage secret.

## Historical certificate evidence

- DNS and TCP 443 resolve/connect, but both Schannel and Node TLS clients receive
  a server-side TLS internal error before any HTTP request reaches AICL.
- One later bounded HTTPS retry timed out after 10 seconds without receiving an
  HTTP response. It was not followed by another certificate request.
- A certificate request writing certificate/key output to Windows `NUL`
  reproduced Tailscale control-plane HTTP 500 while creating the ACME DNS
  challenge. The challenge value is intentionally not recorded here.
- Tailscale's public status page did not list an active Certificates incident at
  the time, so this is treated as tailnet/control-plane support or propagation
  state rather than an AICL defect.

Certificate issuance later recovered and the HTTPS path now passes. No further
`tailscale cert` invocation was required.

## Remaining blocker

- The only online peer observed is `POCO C65`, an Android device.
- The host-side browser preflight proves the remote path but is not second-device
  evidence.
- `reviews/codex/M8.5-SECOND-DEVICE.json` must not exist until the supplied probe
  passes on another online device in the same tailnet.

## Operator decision

On 2026-08-03 the operator deferred second-device acceptance and allowed M8.6
to become current. This is not a waiver or pass. Google identity plus Cloudflare
is planned as replacement remote-access work, but the identity boundary remains
unresolved and no Cloudflare or Google authentication has been implemented.

## Required external completion if resumed

1. Open `https://bluewhalex.tailc79f02.ts.net` on `POCO C65` and confirm the UI
   reports both Core online and Connector ready, or use another tailnet computer.
2. On a second Windows tailnet device, run `Test-TailscaleRemote.ps1 -Origin
   https://<device>.<tailnet>.ts.net -EvidencePath
   reviews/codex/M8.5-SECOND-DEVICE.json`.
3. Transfer the generated evidence file back to this repository. For Android,
   capture equivalent operator evidence before replacing the scripted gate.
4. Only then mark M8.5 complete and advance to M8.6.
