# M8.5 Deployment Readiness

Status: **BLOCKED — implementation verified; real tailnet evidence absent**

## Implemented

- `pnpm remote:configure` requires AICL stopped, requires Core on
  `127.0.0.1`, derives one exact `https://*.ts.net` Origin from online Tailscale
  status, configures only private Serve, verifies the loopback target, then
  persists the Origin atomically.
- `pnpm remote:status` distinguishes app, Connector, Tailscale, Serve, and
  Origin state. Doctor additionally distinguishes both SQLite databases and
  Codex compatibility.
- `Test-TailscaleRemote.ps1` refuses execution on the server device, tests
  production HTML, `/health`, the bodyless runtime bootstrap, and authenticated
  WSS, and never writes the one-time ticket to evidence.
- No implementation invokes `tailscale funnel`, opens a LAN listener, or changes
  Core/Connector away from loopback.

## Verification

```text
apps/host typecheck/test/lint             PASS (8 tests)
Test-TailscaleAutomation.ps1              PASS (fake online CLI/Serve)
pnpm build                                PASS
pnpm run doctor                           READY; local DB/Codex/app pass
pnpm remote:status                        app/Connector online; Tailscale missing
pnpm check                                PASS (100 tests + lifecycle/Serve smokes)
git diff --check                          PASS
```

The first full check encountered `EPERM` while deleting an M8.3 temporary
process-test directory on Windows. The same unchanged targeted test passed
immediately, and the complete gate then passed. No test or timeout was weakened.

## Blocking evidence

- `tailscale.exe` is absent from PATH and the usual Program Files/LocalAppData
  locations; no Tailscale Windows service was discovered.
- Consequently no real ts.net Origin, HTTPS certificate, Serve mapping, or
  remote WSS result is available.
- `reviews/codex/M8.5-SECOND-DEVICE.json` must not exist until the supplied probe
  passes on another online device in the same tailnet.

## Required external completion

1. Install and sign in to Tailscale on the host and a second device; enable
   tailnet HTTPS certificates and confirm ACL access.
2. Stop the dev/production stack, run `pnpm remote:configure`, then `pnpm start`.
3. Confirm `pnpm remote:status` and `pnpm run doctor` report online/configured.
4. On the second device, run `Test-TailscaleRemote.ps1 -Origin
   https://<device>.<tailnet>.ts.net -EvidencePath
   reviews/codex/M8.5-SECOND-DEVICE.json`.
5. Only then mark M8.5 complete and advance to M8.6.
